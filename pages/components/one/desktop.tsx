import React from 'react';
import withTransition from '@lib/withTransition';

const CompOneDesktop = () => {
  return (
    <div>
      <p>todo</p>
    </div>
  );
};

export default withTransition(CompOneDesktop);
